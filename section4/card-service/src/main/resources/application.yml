spring:
  application:
    name: card-service
  datasource:
    url: jdbc:h2:mem:bank-project
    driver-class-name: org.h2.Driver
    hikari:
      username: sa
      password:
  h2:
    console:
      enabled: true
      path: /h2-console
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
server:
  port: 8081
